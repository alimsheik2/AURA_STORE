import { useI18n } from '@/contexts/I18nContext';
import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { Store, Mail, Lock, User, Loader2, ShoppingBag, Truck, FileUp } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import type { UserRole } from '@/lib/types';
import { classNames } from '@/lib/utils';

export default function SignUp() {
  const { t } = useI18n();
  const { signUp, verifySignupOtp, submitVendorKyc } = useAuth(); const navigate = useNavigate();
  const [fullName,setFullName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [phone,setPhone]=useState(''); const [countryCode,setCountryCode]=useState('IQ'); const [role,setRole]=useState<UserRole>('customer'); const [document,setDocument]=useState<File|null>(null); const [legalName,setLegalName]=useState(''); const [documentType,setDocumentType]=useState('identity');
  const [error,setError]=useState<string|null>(null); const [loading,setLoading]=useState(false); const [otpMode,setOtpMode]=useState(false); const [otp,setOtp]=useState('');
  const handleSubmit=async(e:React.FormEvent)=>{e.preventDefault();setError(null);setLoading(true);if(role==='vendor'&&(!phone||!document||!legalName)){setLoading(false);setError('Vendor accounts require phone, legal name and an identity/business document.');return;}const r=await signUp(email,password,fullName,role,phone,countryCode);setLoading(false);if(r.error)setError(r.error);else if(r.needsOtp)setOtpMode(true);};
  const verify=async(e:React.FormEvent)=>{e.preventDefault();setError(null);setLoading(true);const r=await verifySignupOtp(email,otp);if(r.error){setLoading(false);setError(r.error);return;}if(role==='vendor'){if(!document){setLoading(false);setError('Document is required');return;}const k=await submitVendorKyc({legalName,phone,countryCode,document,documentType});if(k.error){setLoading(false);setError(k.error);return;}setLoading(false);navigate('/vendor');}else{setLoading(false);navigate('/');}};
  if(otpMode)return <div className="min-h-screen bg-gradient-to-br from-slate-50 to-sky-50 flex items-center justify-center px-4"><div className="w-full max-w-md bg-white rounded-2xl shadow-xl border border-gray-100 p-8"><h1 className="text-2xl font-bold text-gray-900 mb-2">{t('auth.verifyEmail')}</h1><p className="text-sm text-gray-500 mb-6">{t('auth.verifyCode',{email})} {role==='vendor'&&t('auth.kycNotice')}</p>{error&&<div className="mb-4 p-3 bg-rose-50 border border-rose-200 rounded-lg text-sm text-rose-600">{error}</div>}<form onSubmit={verify} className="space-y-4"><input inputMode="numeric" pattern="[0-9]{6}" maxLength={6} required value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,'').slice(0,6))} className="w-full h-14 text-center tracking-[0.6em] text-2xl rounded-lg border border-gray-300" placeholder="000000"/><button disabled={loading||otp.length!==6} className="w-full h-11 bg-sky-500 text-white font-medium rounded-lg disabled:opacity-50 flex items-center justify-center gap-2">{loading&&<Loader2 size={18} className="animate-spin"/>}Verify</button></form></div></div>;
  return <div className="min-h-screen bg-gradient-to-br from-slate-50 to-sky-50 flex items-center justify-center px-4 py-8"><div className="w-full max-w-md"><div className="bg-white rounded-2xl shadow-xl border border-gray-100 p-8"><div className="flex flex-col items-center mb-8"><div className="w-14 h-14 bg-gradient-to-br from-sky-500 to-blue-600 rounded-2xl flex items-center justify-center mb-3"><Store className="text-white" size={28}/></div><h1 className="text-2xl font-bold text-gray-900">{t('auth.createAccount')}</h1><p className="text-sm text-gray-500 mt-1">{t('auth.onboarding')}</p></div>{error&&<div className="mb-4 p-3 bg-rose-50 border border-rose-200 rounded-lg text-sm text-rose-600">{error}</div>}<div className="grid grid-cols-2 gap-3 mb-6"><button type="button" onClick={()=>setRole('customer')} className={classNames('p-4 rounded-xl border-2 text-center',role==='customer'?'border-sky-500 bg-sky-50':'border-gray-200')}><ShoppingBag className="mx-auto mb-2 text-sky-500" size={24}/><p className="text-sm font-medium">{t('auth.customer')}</p></button><button type="button" onClick={()=>setRole('vendor')} className={classNames('p-4 rounded-xl border-2 text-center',role==='vendor'?'border-sky-500 bg-sky-50':'border-gray-200')}><Truck className="mx-auto mb-2 text-sky-500" size={24}/><p className="text-sm font-medium">{t('auth.vendor')}</p></button></div><form onSubmit={handleSubmit} className="space-y-4"><input required value={fullName} onChange={e=>setFullName(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300" placeholder={t('auth.fullName')}/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300" placeholder={t('auth.emailPlaceholder')}/><input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300" placeholder={t('auth.passwordHint')}/>{role==='vendor'&&<><input required value={legalName} onChange={e=>setLegalName(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300" placeholder={t('auth.legalName')}/><input required value={phone} onChange={e=>setPhone(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300" placeholder={t('auth.phone')}/><select value={countryCode} onChange={e=>setCountryCode(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300"><option value="IQ">{t('country.iq')}</option><option value="SA">{t('country.sa')}</option><option value="AE">{t('country.ae')}</option><option value="JO">{t('country.jo')}</option><option value="KW">{t('country.kw')}</option><option value="QA">{t('country.qa')}</option><option value="LB">{t('country.lb')}</option><option value="EG">{t('country.eg')}</option></select><select value={documentType} onChange={e=>setDocumentType(e.target.value)} className="w-full h-11 px-3 rounded-lg border border-gray-300"><option value="identity">{t('auth.identityDocument')}</option><option value="business_registration">{t('auth.businessRegistration')}</option></select><label className="flex items-center gap-3 p-3 border border-dashed border-gray-300 rounded-lg cursor-pointer"><FileUp className="text-sky-500" size={20}/><span className="text-sm text-gray-600">{document?.name||t('auth.uploadDocument')}</span><input type="file" accept="image/*,.pdf" required onChange={e=>setDocument(e.target.files?.[0]??null)} className="hidden"/></label></>}<button type="submit" disabled={loading} className="w-full h-11 bg-sky-500 text-white font-medium rounded-lg disabled:opacity-50 flex items-center justify-center gap-2">{loading&&<Loader2 size={18} className="animate-spin"/>}{t('auth.createAccount')}</button></form><p className="text-center text-sm text-gray-500 mt-6">{t('auth.alreadyAccount')} <Link to="/signin" className="text-sky-600 font-medium">{t('auth.signin')}</Link></p></div></div></div>;
}
